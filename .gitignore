node_modules/
dist/
*.log

# Installer runtime artifacts (generated by `aai install`)
/docker-compose.yml
/aai.json
/data/
/config/
/templates/
# Saved remote SSH targets (infra coordinates + key paths — not secrets, but private)
/targets/

.DS_Store
